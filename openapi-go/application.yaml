# Generated by lark-openapi-spec — DO NOT EDIT BY HAND.
# Source: lark-cli Go source (shortcuts/), version 1.0.76
openapi: 3.0.3
info:
  title: 飞书开放平台 · application API（lark-cli Go 源码）
  description: |-
    从 lark-cli Go 源码 `shortcuts/application/` 提取的 HTTP 接口定义，含官方 API Explorer 未收录的接口。

    ---

    本文档由 [lark-openapi-spec](https://github.com/dayongxie/lark-openapi-spec) 项目自动提取（启发式，非官方发布物），字段为近似推断，权威说明以[飞书开放平台](https://open.feishu.cn/document)为准。
  version: 1.0.76
  x-lark-service: application
  x-lark-track: go (lark-cli source)
  x-generator: lark-openapi-spec tools/extract_go_apis.py
servers:
- url: https://open.feishu.cn
  description: 飞书（中国大陆）
- url: https://open.larksuite.com
  description: Lark（国际版）
tags:
- name: application
paths:
  /open-apis/application/v7/app_slash_commands:
    get:
      operationId: application_slash_command_update
      summary: Update description / localized descriptions / icon of a slash command on the current bound app, addressed by
        --command-id or by name via --command
      description: |-
        Update description / localized descriptions / icon of a slash command on the current bound app, addressed by --command-id or by name via --command


        **Tips（来自 lark-cli 源码）**

        - lark-cli application +slash-command-update --command greet --description "new text" --as bot
        - PATCH is field-level partial: fields you do not pass are preserved server-side
        - the command NAME itself cannot be changed (API limitation): rename = delete + create (new command_id)
      tags:
      - application
      x-lark-cli-command: lark-cli application +slash-command-update
      x-lark-source: lark-cli-go
      x-lark-risk: write
      x-lark-scopes:
      - application:app_slash_command:write
      x-lark-access-tokens:
      - tenant_access_token
      - user_access_token
      parameters:
      - name: command_id
        in: query
        required: false
        schema:
          type: string
          description: target command_id (from +slash-command-list or create output); mutually exclusive with --command
      - name: command
        in: query
        required: false
        schema:
          type: string
          description: target command name WITHOUT leading slash; resolved via live list (needs read scope); mutually exclusive
            with --command-id
      - name: description
        in: query
        required: false
        schema:
          type: string
          description: new default description (description.default_value)
      - name: description_i18n
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
          description: localized description, repeatable <lang>=<text>; REPLACES the whole i18n map (missing languages are
            dropped); requires --description
      - name: icon_key
        in: query
        required: false
        schema:
          type: string
          description: new icon key (invalid keys rejected server-side with code 40000031)
      responses:
        '200':
          description: 飞书标准响应信封（data 结构未在 lark-cli 源码中声明）
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                  msg:
                    type: string
                  data:
                    type: object
    post:
      operationId: application_slash_command_create
      summary: Register a slash command (/ command) on the current bound Open Platform app; --force converts a name collision
        into an update (idempotent re-run)
      description: |-
        Register a slash command (/ command) on the current bound Open Platform app; --force converts a name collision into an update (idempotent re-run)


        **Tips（来自 lark-cli 源码）**

        - lark-cli application +slash-command-create --command greet --description "say hi" --description-i18n zh_cn=问候 --as bot
        - changes take ~5 minutes to appear in clients (client-side cache); the server updates immediately
        - user identity needs explicit authorization first: lark-cli auth login --scope application:app_slash_command:write
      tags:
      - application
      x-lark-cli-command: lark-cli application +slash-command-create
      x-lark-source: lark-cli-go
      x-lark-risk: write
      x-lark-scopes:
      - application:app_slash_command:write
      x-lark-access-tokens:
      - tenant_access_token
      - user_access_token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                command:
                  type: string
                  description: command name WITHOUT the leading slash (server enforces uniqueness per app; max 100 commands)
                description:
                  type: string
                  description: default description shown in the client command panel (description.default_value)
                description_i18n:
                  type: array
                  items:
                    type: string
                  description: localized description, repeatable, format <lang>=<text> (e.g. zh_cn=发送问候); language codes are
                    passed through to the server
                icon_key:
                  type: string
                  description: 'icon key (server default: skill_outlined; invalid keys are rejected server-side with code
                    40000031)'
              required:
              - command
              - description
      responses:
        '200':
          description: 飞书标准响应信封（data 结构未在 lark-cli 源码中声明）
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                  msg:
                    type: string
                  data:
                    type: object
